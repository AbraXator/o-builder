export enum InteractionModes {
  SELECTING = "selecting",
  PLACING = "placing",
} 

export enum Pages {
  MAIN = "main",
  CREATE_COURSE = "createCourse",
  RECENT = "recent",
  MAP = "map",
  ITEMS = "items",
  CONTROLS = "controls",
  ROUTES = "routes",
}

export enum ControlTypes {
  START = "start",
  CONTROL = "control",
  FINISH = "finish"
}